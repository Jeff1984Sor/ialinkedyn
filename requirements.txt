# --- Web / API ---
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
gunicorn>=22.0,<23.0

# --- Banco / ORM / Migrations ---
sqlalchemy>=2.0,<2.1
alembic>=1.13,<2.0
psycopg[binary]>=3.1,<4.0

# --- Validação / Config ---
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
pydantic[email]>=2.7,<3.0

# --- Auth / Segurança ---
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
bcrypt==4.0.1  # passlib 1.7.4 quebra com bcrypt>=4.1 (licao herdada do MayaPost)
cryptography>=42.0  # Fernet (tokens de terceiros)

# --- IA (agentes) — o motor é escolhido no painel ---
google-genai>=1.0,<2.0
openai>=1.40,<3.0

# --- Worker (agendamento) ---
apscheduler>=3.10,<4.0

# --- HTTP (provedor LinkedIn: Unipile) ---
httpx>=0.27,<1.0

# --- Utilitarios ---
python-multipart>=0.0.9  # uploads (avatar da funcionaria, midias de post)
