import { EmConstrucao } from "@/components/EmConstrucao";

export default function Page() {
  return (
    <EmConstrucao
      titulo="Prospecção"
      descricao="Defina o cliente ideal (ICP), busque empresas/pessoas e gere abordagens personalizadas a partir do perfil (agente Caçador), com limites anti-ban."
    />
  );
}
