import { EmConstrucao } from "@/components/EmConstrucao";

export default function Page() {
  return (
    <EmConstrucao
      titulo="Marca / Voz"
      descricao="Nome e persona da sua funcionária, avatar, tom da empresa, público ideal (ICP) e CTA."
    />
  );
}
